# Unscented Kalman Filter — runtime dependencies
# Install with:  pip install -r requirements.txt

numpy>=1.21        # core linear algebra, sigma point arithmetic
scipy>=1.7         # scipy.linalg.cholesky — matrix square root for sigma points
pandas>=1.3        # CSV loading of the vehicle log
matplotlib>=3.4    # result figures
